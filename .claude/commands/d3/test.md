> **Deprecated — `/test` is now `/prove --tests` (D3 v2).** This stub forwards for one release; please switch.

A dedicated test pass is PROVE with --tests.

---

**Forward:** treat this invocation as `/prove --tests` with the same arguments. Print the one-line deprecation notice above, then run `/prove --tests` (load `.claude/commands/d3/prove.md`) with the user's input unchanged.
