> **Deprecated — `/d3:test` is now `/d3:prove --tests` (D3 v2).** This stub forwards for one release; please switch.

A dedicated test pass is PROVE with --tests.

---

**Forward:** treat this invocation as `/d3:prove --tests` with the same arguments. Print the one-line deprecation notice above, then run `/d3:prove --tests` (load `.claude/commands/d3/prove.md`) with the user's input unchanged.
