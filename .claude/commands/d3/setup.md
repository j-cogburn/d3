> **Deprecated — `/d3:setup` is now `/d3:define --project` (D3 v2).** This stub forwards for one release; please switch.

Project bootstrap is project-level DEFINE.

---

**Forward:** treat this invocation as `/d3:define --project` with the same arguments. Print the one-line deprecation notice above, then run `/d3:define --project` (load `.claude/commands/d3/define.md`) with the user's input unchanged.
