> **Deprecated — `/improve` is now `/build --quality` (D3 v2).** This stub forwards for one release; please switch.

A scoped quality loop is a BUILD flag.

---

**Forward:** treat this invocation as `/build --quality` with the same arguments. Print the one-line deprecation notice above, then run `/build --quality` (load `.claude/commands/d3/build.md`) with the user's input unchanged.
