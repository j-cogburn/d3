> **Deprecated — `/d3:spec` is now `/d3:define` (D3 v2).** This stub forwards for one release; please switch.

Requirements gathering is defining the objective.

---

**Forward:** treat this invocation as `/d3:define` with the same arguments. Print the one-line deprecation notice above, then run `/d3:define` (load `.claude/commands/d3/define.md`) with the user's input unchanged.
