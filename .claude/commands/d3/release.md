> **Deprecated — `/d3:release` is now `/d3:build --release` (D3 v2).** This stub forwards for one release; please switch.

Staged rollout is the release path of BUILD.

---

**Forward:** treat this invocation as `/d3:build --release` with the same arguments. Print the one-line deprecation notice above, then run `/d3:build --release` (load `.claude/commands/d3/build.md`) with the user's input unchanged.
