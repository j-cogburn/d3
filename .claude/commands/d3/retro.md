> **Deprecated — `/d3:retro` is now `/d3:learn --retro` (D3 v2).** This stub forwards for one release; please switch.

Retrospective is calibration inside LEARN.

---

**Forward:** treat this invocation as `/d3:learn --retro` with the same arguments. Print the one-line deprecation notice above, then run `/d3:learn --retro` (load `.claude/commands/d3/learn.md`) with the user's input unchanged.
