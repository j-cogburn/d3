> **Deprecated — `/evaluate` is now `/learn` (D3 v2).** This stub forwards for one release; please switch.

The unified dashboard is `/learn` with no argument.

---

**Forward:** treat this invocation as `/learn` with the same arguments. Print the one-line deprecation notice above, then run `/learn` (load `.claude/commands/d3/learn.md`) with the user's input unchanged.
