> **Deprecated — `/d3:evaluate` is now `/d3:learn` (D3 v2).** This stub forwards for one release; please switch.

The unified dashboard is `/d3:learn` with no argument.

---

**Forward:** treat this invocation as `/d3:learn` with the same arguments. Print the one-line deprecation notice above, then run `/d3:learn` (load `.claude/commands/d3/learn.md`) with the user's input unchanged.
