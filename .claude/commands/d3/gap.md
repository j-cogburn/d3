> **Deprecated — `/d3:gap` is now `/d3:learn gaps` (D3 v2).** This stub forwards for one release; please switch.

Product gap analysis is a LEARN sensing dimension that feeds SHAPE.

---

**Forward:** treat this invocation as `/d3:learn gaps` with the same arguments. Print the one-line deprecation notice above, then run `/d3:learn gaps` (load `.claude/commands/d3/learn.md`) with the user's input unchanged.
