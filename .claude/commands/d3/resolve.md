> **Deprecated — `/d3:resolve` is now `/d3:sprint --quick` (D3 v2).** This stub forwards for one release; please switch.

The fast lightweight path is `/d3:sprint --quick`.

---

**Forward:** treat this invocation as `/d3:sprint --quick` with the same arguments. Print the one-line deprecation notice above, then run `/d3:sprint --quick` (load `.claude/commands/d3/sprint.md`) with the user's input unchanged.
