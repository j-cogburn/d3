> **Deprecated — `/resolve` is now `/sprint --quick` (D3 v2).** This stub forwards for one release; please switch.

The fast lightweight path is `/sprint --quick`.

---

**Forward:** treat this invocation as `/sprint --quick` with the same arguments. Print the one-line deprecation notice above, then run `/sprint --quick` (load `.claude/commands/d3/sprint.md`) with the user's input unchanged.
