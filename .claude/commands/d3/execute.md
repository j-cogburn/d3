> **Deprecated — `/d3:execute` is now `/d3:build` (D3 v2).** This stub forwards for one release; please switch.

Executing ready directives is BUILD.

---

**Forward:** treat this invocation as `/d3:build` with the same arguments. Print the one-line deprecation notice above, then run `/d3:build` (load `.claude/commands/d3/build.md`) with the user's input unchanged.
