> **Deprecated — `/plan` is now `/shape` (D3 v2).** This stub forwards for one release; please switch.

Turning an objective into directives is SHAPE.

---

**Forward:** treat this invocation as `/shape` with the same arguments. Print the one-line deprecation notice above, then run `/shape` (load `.claude/commands/d3/shape.md`) with the user's input unchanged.
