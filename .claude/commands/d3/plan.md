> **Deprecated — `/d3:plan` is now `/d3:shape` (D3 v2).** This stub forwards for one release; please switch.

Turning an objective into directives is SHAPE.

---

**Forward:** treat this invocation as `/d3:shape` with the same arguments. Print the one-line deprecation notice above, then run `/d3:shape` (load `.claude/commands/d3/shape.md`) with the user's input unchanged.
