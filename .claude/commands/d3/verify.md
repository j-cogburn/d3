> **Deprecated — `/verify` is now `/prove` (D3 v2).** This stub forwards for one release; please switch.

Verification against the objective is PROVE.

---

**Forward:** treat this invocation as `/prove` with the same arguments. Print the one-line deprecation notice above, then run `/prove` (load `.claude/commands/d3/prove.md`) with the user's input unchanged.
