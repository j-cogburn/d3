> **Deprecated — `/setup` is now `/define --project` (D3 v2).** This stub forwards for one release; please switch.

Project bootstrap is project-level DEFINE.

---

**Forward:** treat this invocation as `/define --project` with the same arguments. Print the one-line deprecation notice above, then run `/define --project` (load `.claude/commands/d3/define.md`) with the user's input unchanged.
