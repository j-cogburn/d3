> **Deprecated — `/wireframe` is now `/sketch ui` (D3 v2).** This stub forwards for one release; please switch.

Wireframes are the UI domain of the multi-domain SHAPE sketch system.

---

**Forward:** treat this invocation as `/sketch ui` with the same arguments. Print the one-line deprecation notice above, then run `/sketch ui` (load `.claude/commands/d3/sketch.md`) with the user's input unchanged.
