> **Deprecated — `/research` is now `/learn market` (D3 v2).** This stub forwards for one release; please switch.

Generative market research is a LEARN sensing dimension.

---

**Forward:** treat this invocation as `/learn market` with the same arguments. Print the one-line deprecation notice above, then run `/learn market` (load `.claude/commands/d3/learn.md`) with the user's input unchanged.
