> **Deprecated — `/retro` is now `/learn --retro` (D3 v2).** This stub forwards for one release; please switch.

Retrospective is calibration inside LEARN.

---

**Forward:** treat this invocation as `/learn --retro` with the same arguments. Print the one-line deprecation notice above, then run `/learn --retro` (load `.claude/commands/d3/learn.md`) with the user's input unchanged.
