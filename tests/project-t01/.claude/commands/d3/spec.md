> **Deprecated — `/spec` is now `/define` (D3 v2).** This stub forwards for one release; please switch.

Requirements gathering is defining the objective.

---

**Forward:** treat this invocation as `/define` with the same arguments. Print the one-line deprecation notice above, then run `/define` (load `.claude/commands/d3/define.md`) with the user's input unchanged.
