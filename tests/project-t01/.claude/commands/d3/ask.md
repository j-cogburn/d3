> **Deprecated — `/ask` is now `/d3` (D3 v2).** This stub forwards for one release; please switch.

The universal front door is now `/d3`.

---

**Forward:** treat this invocation as `/d3` with the same arguments. Print the one-line deprecation notice above, then run `/d3` (load `.claude/commands/d3/d3.md`) with the user's input unchanged.
